print-ruby-configure-args:

	prints the compile time and configuration arguments used when ruby was 
	compiled

update-ruby-environment:

	updates rbenv, ruby-build, and rbenv-gem-rehash

ruby-install:

	A simple wrapper around ruby-build to facilitate building and 
	installing ruby with CFLAGS and the openssl available through homebrew.
	
	Installs to /opt/rubies/ 

	prerequisites:
		- bash-completion
		- ruby-build

	How-To:
		- run sh install.sh
		(scripts install to ~/bin and completion scripts to 
		 /usr/local/etc/bash_completion.d/)
		
		- ruby-install [version]
		will autocomplete to the versions available from ruby-build
